{% if reusableComponent %}
	{% for component in reusableComponent.meta('reusableComponents') %}
		{{ renderComponent(component) }}
	{% endfor %}
{% endif %}
{% if reusableBlock %}
	{% for component in reusableBlock.meta('reusableBlocks') %}
		{{ renderComponent(component) }}
	{% endfor %}
{% endif %}
