{% set heading = model.heading %}
{% set table = model.table %}
{% set options = options is defined ? options : model.options %}


<flynt-component name="SectionPricingCards" {{ options.theme ? 'data-theme="' ~ options.theme ~ '"' }} class="edge sand bg-shade dark:bg-dark">
	<section class="tw-section flex flex-col gap-8 lg:gap-12">
		{% if heading %}
			{% include '@Components/Heading/index.twig' with { model: heading } only %}
		{% endif %}

		<div class="bg-white border border-border-card rounded-3xl dark:bg-dark-shade dark:border-0 overflow-hidden">
			<div class="flex flex-col justify-center px-4 py-6 md:p-8 lg:p-10">

				{% include '@Components/Table/index.twig' with {
						model: table,
						options: options
				} only %}

			</div>
		</div>
	</section>
	{% include '@Components/Common/_json.twig' with {value: model, varName: 'model'} %}
</flynt-component>
